apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aks-demo-pvc
  namespace: aks-demo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi
